/**
 * Seed: Dados de exemplo em inglês
 * (Migração SQLite desabilitada)
 */

exports.seed = async function(knex) {
  console.log('⚠️  Seed de migração SQLite desabilitado.')
  console.log('   Este seed foi desabilitado - dados serão inseridos por outros seeds.')
  
  // Não fazer nada - outros seeds vão inserir os dados
  return Promise.resolve()
}